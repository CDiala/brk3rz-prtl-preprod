import { AttachmentModel } from './attachment.model';

describe('AttachmentModel', () => {
  it('should create an instance', () => {
    const instance: AttachmentModel = {} as AttachmentModel;
    expect(instance).toBeTruthy();
  });
});
